# 抽样方式与严格漏斗学习记录

事件级随机抽样适合估计行为分布、日活和行为占比，但会破坏用户行为序列。对事件随机 1M 样本做严格 `user_id + item_id` 漏斗时，浏览后意向只有 185 对、之后购买只有 1 对，不能当作真实转化结论。

用户级抽样则先按 `user_id` 固定哈希选取约 1% 用户，再保留这些用户的全部事件。它产生 976,621 行、9,660 个完整用户，并得到可解释的严格漏斗：PV 对 691,122、意向对 11,101、购买对 1,023。两类样本服务不同目的，不能混用。

